name: workflowcall-pipeline
on: [push]

jobs:
  feature-pipeline:
  uses: devopsactios/workflows/.github/workflows/nodejs-feature-pipeline.yaml
  with:
    project: 'roboshop'
    component: 'catalogue'
    environment: 'dev' 
    
