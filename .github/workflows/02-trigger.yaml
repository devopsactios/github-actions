name: Trigger
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: manual dispatch
        run: echo   "manually dispatched"
